export class Width {
  public fullWidth:number;
  public containerWidth:number;


  constructor(fullWidth: number, containerWidth: number) {
    this.fullWidth = fullWidth;
    this.containerWidth = containerWidth;
  }
}
